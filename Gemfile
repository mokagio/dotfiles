source 'https://rubygems.org'

# This has only commented stuff at the moment, but I can't bring myself to get rid of it

# a cli tool to set the desktop image across **all** the desktops
# gem 'desktop'

gem 'ruby-lsp'

# Bundled (not standalone) so setup can build its shell completion via
# `fastlane enable_auto_complete` after `bundle install`.
gem 'fastlane'
