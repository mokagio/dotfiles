# Starship prompt — replaces spaceship-prompt + spaceship_verbose_git.zsh

# Show a blank line between prompts
add_newline = true

[character]
# possible symbols to consider:
# ❯
# ➜
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"

# Right prompt: time
[time]
disabled = true
format = "[$time]($style) "

[cmd_duration]
min_time = 1_000
format = "[took $duration]($style) "

# Git branch in magenta, no prefix
[git_branch]
format = "[$branch(:$remote_branch)]($style) "
style = "magenta"

# Git status with counts and thin symbols.
# Starship has no join/separator for status items, so each symbol carries a
# trailing space.
# This means the last item leaves a space before ']' — accepted trade-off.
# https://github.com/starship/starship/issues/1724
[git_status]
format = '([\[$all_status$ahead_behind\]](bold red) )'
style = "bold red"
ahead = "⇡${count} "
behind = "⇣${count} "
conflicted = "≠ "
stashed = "⚑${count} "
deleted = "✘${count} "
renamed = "»${count} "
modified = "!${count} "
staged = "+${count} "
untracked = "?${count} "
up_to_date = ""

# Ruby version — no prefix clutter
[ruby]
format = "[$version]($style) "

# Hide modules we don't want
[docker_context]
disabled = true

[gradle]
disabled = true

[java]
disabled = true

[lua]
disabled = true

[swift]
disabled = true
